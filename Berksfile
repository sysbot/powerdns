site :opscode

metadata

cookbook 'mariadb', :path => "#{ENV['CHEF_REPO']}/cookbooks/mariadb"
cookbook 'ooyala-mariadb', :path => "#{ENV['CHEF_REPO']}/site-cookbooks/ooyala-mariadb"
cookbook 'database', :path =>  "#{ENV['CHEF_REPO']}/cookbooks/database"
cookbook 'resolvconf', :path =>  "#{ENV['CHEF_REPO']}/cookbooks/resolvconf"
cookbook 'sqlite'

